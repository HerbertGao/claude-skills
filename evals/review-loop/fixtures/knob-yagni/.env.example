# 复制为 .env 后按部署环境调整。

# operator 手工维护的 rules.yaml 所在目录（只读挂载）
CONFIG_DIR=/etc/mailsift

# 机器维护的状态目录
DATA_DIR=/var/lib/mailsift

# 单批处理的邮件条数
BATCH_SIZE=200

# 出站中继
SMTP_HOST=relay.internal

# trace | debug | info | warn | error
LOG_LEVEL=info
